# Web UI Improvement Tasks

Tasks are executed in order. A task is complete only when every Gate is checked, and it
moves to [docs/archive/web-ui-tasks.md](docs/archive/web-ui-tasks.md) once it does.

No open tasks.
